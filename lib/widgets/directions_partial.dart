import 'package:flutter/material.dart';

class DirectionsWidget extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
     return Center(
        child: Text('Directions Partial'),
       );
  }
}